# 네이버 블로그 자동 발행 가이드

AI가 생성한 DSL 콘텐츠를 네이버 블로그에 발행하는 방법을 안내합니다.

## 목차

- [시스템 개요](#시스템-개요)
- [DSL 형식 이해](#dsl-형식-이해)
- [자동 생성 흐름](#자동-생성-흐름)
- [네이버 블로그 발행](#네이버-블로그-발행)
- [CLI 도구 사용](#cli-도구-사용)
- [웹 UI 사용](#웹-ui-사용)
- [문제 해결](#문제-해결)

---

## 시스템 개요

### 전체 프로세스

```
1. 예약 크롤링 실행
   ↓
2. 타겟 블로그 확인
   ↓
3. [타겟 없음] → AI 블로그 글 생성 (DSL 형식)
   ↓
4. 데이터베이스 저장
   ↓
5. 웹 UI 또는 CLI로 확인
   ↓
6. 네이버 블로그에 수동 발행
   ↓
7. 발행 완료 표시
```

### 왜 DSL 형식인가?

**네이버 블로그 스마트에디터**는 자체 DSL(Domain Specific Language)을 사용합니다:

- ✅ **호환성**: 네이버 블로그와 100% 호환
- ✅ **일관성**: 플랫폼 간 서식 차이 없음
- ✅ **안정성**: Markdown보다 안정적인 변환
- ✅ **SEO**: 네이버 검색에 최적화

---

## DSL 형식 이해

### 기본 태그

| DSL 태그 | 설명 | 예시 |
|----------|------|------|
| `[title]...[/title]` | 글 제목 | `[title]파이썬 강의 완벽 가이드[/title]` |
| `[bold]...[/bold]` | 굵게 (소제목) | `[bold]1. 파이썬이란?[/bold]` |
| `[underline]...[/underline]` | 밑줄 (키워드) | `[underline]파이썬[/underline]` |
| `[italic]...[/italic]` | 기울임 | `[italic]부가 설명[/italic]` |
| `[separator=LINE][/separator]` | 구분선 | `[separator=line3][/separator]` |
| `[quote=TEXT]` | 인용구 | `[quote=핵심 포인트!]` |
| `[align=center]...[/align]` | 정렬 | `[align=center]중앙 정렬[/align]` |
| `[img=1,2 layout=collage][/img]` | 이미지 | `[img=1,2 layout=collage][/img]` |

### 구분선 종류

- `line1` - 얇은 실선
- `line2` - 중간 실선
- `line3` - 굵은 실선 (기본)
- `line4` - 점선
- `line5` - 이중선
- `line6` - 파선
- `line7` - 장식선

### 예시 DSL 콘텐츠

```
[title]초보자를 위한 파이썬 강의 완벽 가이드 2024[/title]
[separator=line3][/separator]

안녕하세요! 오늘은 [underline]파이썬 강의[/underline]를 처음 시작하시는 분들을 위한 완벽한 가이드를 준비했습니다.

[separator=line3][/separator]
[bold]1. 파이썬이란?[/bold]

파이썬은 초보자도 쉽게 배울 수 있는 프로그래밍 언어입니다. 문법이 간결하고 직관적이어서 프로그래밍을 처음 접하는 분들에게 특히 추천드립니다.

[quote=파이썬은 'Life is short, You need Python'이라는 철학을 가지고 있습니다]

[separator=line3][/separator]
[bold]2. 파이썬 강의 선택 기준[/bold]

좋은 파이썬 강의를 선택하는 3가지 기준을 알려드리겠습니다...
```

---

## 자동 생성 흐름

### 1. 타겟 블로그 등록

```
키워드: "파이썬 강의"
타겟 블로그: blog.naver.com/myid
```

### 2. 스케줄 크롤링 실행

```
매일 오전 9시 자동 크롤링
  ↓
네이버 검색 "파이썬 강의"
  ↓
검색 결과 1-30위 수집
```

### 3. 타겟 블로그 확인

```python
# 검색 결과에서 확인
for result in crawl_results:
    if "blog.naver.com/myid" in result.url:
        # ✅ 타겟 발견!
        return  # AI 글 생성 안 함

# ⚠️ 타겟 없음!
# → AI 블로그 글 자동 생성
```

### 4. AI 글 생성

```
🤖 OpenAI GPT-3.5-turbo 호출
  ↓
DSL 형식 블로그 글 생성
  - 제목: [title]...[/title]
  - 본문: 1200-1600자
  - 태그: ["태그1", "태그2", ...]
  ↓
데이터베이스 저장 (status: 'generated')
  ↓
📱 SMS 알림 발송
```

---

## 네이버 블로그 발행

### 방법 1: 웹 UI 사용 (권장)

#### Step 1: 생성된 글 확인

1. http://localhost:3000 접속
2. 좌측 메뉴에서 **"🤖 생성된 글"** 클릭
3. 생성된 글 목록 확인

#### Step 2: 글 상세 보기

1. 원하는 글의 **"보기"** 버튼 클릭
2. 모달 창에서 DSL 콘텐츠 확인

#### Step 3: DSL 복사

1. **"DSL 복사"** 버튼 클릭
2. 클립보드에 전체 콘텐츠 복사됨

#### Step 4: 네이버 블로그 발행

1. **네이버 블로그 접속**: https://blog.naver.com
2. **글쓰기 클릭**: 우측 상단
3. **제목 입력**: 모달의 제목 복사하여 붙여넣기
4. **본문 입력**:
   - 스마트에디터에 DSL 콘텐츠 붙여넣기
   - DSL 태그가 자동으로 서식 적용됨
5. **태그 추가**: 하단 태그 입력란에 태그 추가
6. **발행**: "발행하기" 버튼 클릭

#### Step 5: 발행 완료 표시

1. 웹 UI로 돌아와서
2. **"발행 완료"** 버튼 클릭
3. 발행된 블로그 URL 입력 (선택사항)

---

### 방법 2: CLI 사용

#### 발행 대기 글 목록

```bash
python3 naver-blog-publisher.py --list
```

**출력 예시:**
```
📋 발행 대기 글 목록
================================================================================
✅ ID:   1 | 파이썬 강의      | 초보자를 위한 파이썬 강의 완벽 가이드 2024
        생성: 2025-10-10 09:15:23 | 상태: generated
--------------------------------------------------------------------------------
⏳ ID:   2 | 머신러닝         | 머신러닝 기초부터 실전까지 완벽 정리
        생성: 2025-10-10 10:30:45 | 상태: ready_to_publish
================================================================================
```

#### 발행 준비 (HTML/DSL 파일 생성)

```bash
python3 naver-blog-publisher.py --publish 1
```

**생성 파일:**
- `blog_posts/post_1.html` - HTML 미리보기
- `blog_posts/post_1_dsl.txt` - DSL 원본

#### 발행 완료 표시

```bash
python3 naver-blog-publisher.py --mark-published 1 --url "https://blog.naver.com/myid/123456"
```

---

## 웹 UI 사용

### 생성된 글 페이지

#### 화면 구성

```
┌─────────────────────────────────────────────┐
│ 🤖 생성된 블로그 글            [새로고침]     │
├─────────────────────────────────────────────┤
│ 📊 통계                                      │
│ [총 생성: 5] [발행 대기: 3] [발행 완료: 2]   │
├─────────────────────────────────────────────┤
│ 글 목록                                      │
│ ┌─────────────────────────────────────────┐ │
│ │ [생성됨] ID: 1                           │ │
│ │ 초보자를 위한 파이썬 강의...             │ │
│ │ 생성: 2025-10-10 09:15                   │ │
│ │            [보기] [발행 준비] [삭제]     │ │
│ └─────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────┐ │
│ │ [발행 준비] ID: 2                        │ │
│ │ 머신러닝 기초부터...                     │ │
│ │            [보기] [발행 완료] [삭제]     │ │
│ └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

#### 상세 모달

```
┌─────────────────────────────────────────────┐
│ 초보자를 위한 파이썬 강의 완벽 가이드  [✕]  │
├─────────────────────────────────────────────┤
│ [생성됨] 생성: 2025-10-10 09:15             │
│                                             │
│ 📋 요약                                      │
│ ┌─────────────────────────────────────────┐ │
│ │ 파이썬 프로그래밍을 처음 시작하는...     │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 🏷️ 태그                                     │
│ [파이썬] [강의] [초보자] [프로그래밍]       │
│                                             │
│ 📝 DSL 원본 (네이버 블로그용)    [복사]     │
│ ┌─────────────────────────────────────────┐ │
│ │ [title]초보자를 위한...[/title]         │ │
│ │ [separator=line3][/separator]           │ │
│ │ ...                                     │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 👁️ 미리보기 (태그 제거)                     │
│ ┌─────────────────────────────────────────┐ │
│ │ 초보자를 위한...                         │ │
│ │ 안녕하세요! 오늘은...                    │ │
│ └─────────────────────────────────────────┘ │
│                                             │
│ 📖 네이버 블로그 발행 방법                   │
│ 1. 네이버 블로그 접속                        │
│ 2. 글쓰기 → 스마트에디터                     │
│ 3. 제목과 본문 입력                          │
│ ...                                         │
│                                             │
│         [DSL 복사] [발행 완료] [닫기]        │
└─────────────────────────────────────────────┘
```

---

## DSL 형식 이해

### DSL이란?

**DSL (Domain Specific Language)**: 네이버 블로그 스마트에디터에서 사용하는 전용 마크업 언어

### Markdown vs DSL

| 기능 | Markdown | DSL (네이버) |
|------|----------|--------------|
| 제목 | `# 제목` | `[title]제목[/title]` |
| 굵게 | `**텍스트**` | `[bold]텍스트[/bold]` |
| 밑줄 | `<u>텍스트</u>` | `[underline]텍스트[/underline]` |
| 기울임 | `*텍스트*` | `[italic]텍스트[/italic]` |
| 구분선 | `---` | `[separator=line3][/separator]` |
| 인용 | `> 텍스트` | `[quote=텍스트]` |

### DSL 작성 규칙

1. **태그는 반드시 닫아야 함**
   ```
   ✅ [bold]텍스트[/bold]
   ❌ [bold]텍스트
   ```

2. **태그 사이에 불필요한 공백 금지**
   ```
   ✅ [separator=line3][/separator]
   ❌ [separator=line3] [/separator]
   ```

3. **separator는 정확한 값 사용**
   ```
   ✅ [separator=line3][/separator]
   ❌ [separator=line1~7][/separator]
   ```

4. **이미지는 최대 2개**
   ```
   ✅ [img=1,2 layout=collage][/img]
   ❌ [img=1,2,3,4 layout=collage][/img]
   ```

---

## 자동 생성 흐름

### 설정 확인

#### 1. OpenAI API Key 설정

```bash
# .env 파일 확인
cat .env | grep OPENAI_API_KEY

# 없으면 추가
echo "OPENAI_API_KEY=sk-proj-xxxxx" >> .env
```

#### 2. 패키지 설치

```bash
source venv/bin/activate
pip install openai>=1.3.0
```

#### 3. 테이블 생성

```bash
python3 add-generated-posts-table.py
```

### 자동 생성 시나리오

#### 시나리오: "파이썬 강의" 키워드

**설정:**
```
키워드: "파이썬 강의"
타겟 블로그: blog.naver.com/myid
스케줄: 매일 오전 9시
```

**실행:**
```
09:00 - 크롤링 시작
  ↓
09:01 - 검색 결과 수집 (15개)
  ↓
09:02 - 타겟 블로그 확인
  결과: blog.naver.com/otherid (타겟 아님!)
  ↓
09:03 - 🤖 AI 블로그 글 생성 시작
  ↓
09:04 - 생성 완료
  제목: [title]초보자를 위한 파이썬 강의 완벽 가이드 2024[/title]
  분량: 1350자
  태그: ["파이썬", "강의", "초보자", "프로그래밍", "튜토리얼"]
  ↓
09:04 - 데이터베이스 저장
  ↓
09:04 - 📱 SMS 알림
  "[Naver Monitor] 🤖 AI 블로그 글 생성
   키워드: 파이썬 강의
   제목: 초보자를 위한 파이썬 강의 완벽 가이드 2024"
```

---

## 네이버 블로그 발행

### Step-by-Step 가이드

#### 1️⃣ 웹 UI에서 글 확인

```bash
# 브라우저 접속
http://localhost:3000

# 메뉴 선택
좌측 메뉴 → 🤖 생성된 글
```

#### 2️⃣ 글 선택 및 복사

1. 발행할 글의 **"보기"** 버튼 클릭
2. 모달 창에서 **"DSL 복사"** 버튼 클릭
3. 클립보드에 DSL 콘텐츠 복사됨

#### 3️⃣ 네이버 블로그 접속

1. https://blog.naver.com 접속
2. 로그인
3. **"글쓰기"** 버튼 클릭

#### 4️⃣ 스마트에디터에 붙여넣기

1. **제목 입력란**:
   - 모달의 제목 복사 (태그 제거된 버전)
   - 예: `초보자를 위한 파이썬 강의 완벽 가이드 2024`

2. **본문 입력란**:
   - DSL 콘텐츠 전체 붙여넣기
   - **자동으로 서식 적용됨!**
   - 구분선, 굵게, 밑줄 등 자동 변환

3. **태그 입력**:
   - 하단 태그 입력란에 태그 추가
   - 예: `파이썬, 강의, 초보자, 프로그래밍, 튜토리얼`

#### 5️⃣ 발행

1. **미리보기** (선택사항): 우측 미리보기로 확인
2. **카테고리 선택** (선택사항)
3. **공개 설정**: 전체공개 / 이웃공개 / 비공개
4. **발행하기** 버튼 클릭

#### 6️⃣ 완료 표시

1. 발행 성공 후 블로그 URL 복사
2. 웹 UI로 돌아와서
3. **"발행 완료"** 버튼 클릭
4. URL 입력 (선택사항)

---

## CLI 도구 사용

### 발행 준비

```bash
# 1. 발행 대기 글 목록
python3 naver-blog-publisher.py --list

# 2. 특정 글 발행 준비 (HTML/DSL 파일 생성)
python3 naver-blog-publisher.py --publish 1

# 출력:
# ✅ HTML 파일 저장: blog_posts/post_1.html
# ✅ DSL 파일 저장: blog_posts/post_1_dsl.txt
```

### 파일 확인

```bash
# DSL 파일 내용 확인
cat blog_posts/post_1_dsl.txt

# HTML 파일 브라우저로 열기
open blog_posts/post_1.html
```

### 발행 완료 표시

```bash
python3 naver-blog-publisher.py --mark-published 1 --url "https://blog.naver.com/myid/123456"
```

---

## 문제 해결

### Q1: AI 글이 생성되지 않음

**증상:**
```
⚠️ OpenAI 패키지가 설치되지 않았습니다.
```

**해결:**
```bash
pip install openai>=1.3.0
./start-test.sh
```

---

### Q2: DSL 태그가 적용되지 않음

**증상:**
네이버 블로그에 붙여넣었는데 `[title]...[/title]` 그대로 표시됨

**원인:**
- 스마트에디터 모드가 아님
- HTML 모드로 붙여넣음

**해결:**
1. 네이버 블로그 글쓰기에서
2. **"기본 에디터"** 또는 **"스마트에디터"** 모드 확인
3. DSL 콘텐츠는 **기본 텍스트 모드**에 붙여넣기
4. HTML 모드가 아닌지 확인

---

### Q3: 생성된 글 품질이 낮음

**해결:**

1. **프롬프트 수정** (`server-debug.py`의 `generate_blog_post_ai`)
2. **모델 변경**: `gpt-3.5-turbo` → `gpt-4`
3. **길이 조절**: `max_tokens=2500` → `3000`
4. **창의성**: `temperature=0.7` → `0.8`

---

### Q4: 비용이 너무 많이 나옴

**해결:**

1. **OpenAI Dashboard**에서 월 사용량 제한 설정
   - https://platform.openai.com/account/limits
   
2. **생성 빈도 조절**
   - 타겟 블로그를 더 많이 등록하여 생성 빈도 감소
   
3. **모델 변경**
   - `gpt-4` → `gpt-3.5-turbo` (10배 저렴)

---

### Q5: 타겟 블로그가 있는데도 생성됨

**원인:** URL 패턴 불일치

**확인:**
```bash
# 크롤링 결과 URL
sqlite3 naver_monitor.db "SELECT url FROM crawl_results WHERE crawl_run_id = 1 LIMIT 5;"

# 타겟 블로그 URL 패턴
sqlite3 naver_monitor.db "SELECT url_pattern FROM blogs;"
```

**수정:**
```bash
sqlite3 naver_monitor.db "UPDATE blogs SET url_pattern = 'blog.naver.com/myid' WHERE id = 1;"
```

---

## API 참고

### 엔드포인트

```bash
# 생성된 글 목록
GET /api/generated-posts?limit=50

# 특정 키워드의 글
GET /api/generated-posts?keyword_id=1

# 글 상세
GET /api/generated-posts/{post_id}

# 발행 준비
POST /api/generated-posts/{post_id}/prepare-publish

# 발행 완료 표시
POST /api/generated-posts/{post_id}/mark-published

# 글 삭제
DELETE /api/generated-posts/{post_id}
```

---

## 모범 사례

### ✅ 권장 흐름

1. **AI 생성 확인**: 웹 UI에서 생성된 글 확인
2. **내용 검토**: DSL 원본과 미리보기 확인
3. **수정 필요 시**: 데이터베이스에서 직접 수정 또는 재생성
4. **네이버 발행**: DSL 복사 → 네이버 블로그 붙여넣기
5. **완료 표시**: 발행 완료 버튼 클릭

### 💡 팁

1. **일괄 발행**: 여러 글을 한 번에 확인 후 발행
2. **카테고리 활용**: 네이버 블로그 카테고리별 분류
3. **예약 발행**: 네이버 블로그 예약 발행 기능 활용
4. **수동 수정**: 발행 전 내용 일부 수정 가능

---

## 참고 자료

- [OpenAI API 문서](https://platform.openai.com/docs)
- [네이버 블로그 고객센터](https://help.naver.com/service/30003/category/30003)
- [AI_BLOG_WRITER.md](AI_BLOG_WRITER.md) - AI 블로그 작성 상세 가이드

---

**마지막 업데이트:** 2025-10-10  
**버전:** 1.0.0

